{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "rm -rf node_modules package-lock.json npm install --legacy-peer-dep",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
}